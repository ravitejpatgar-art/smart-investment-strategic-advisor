import React, { forwardRef } from 'react';import { cn } from '../../utils/cn';export interface CardProps extends React.HTMLAttributes<HTMLDivElement> {  variant?: 'default' | 'elevated' | 'interactive' | 'accent';  padding?: 'none' | 'sm' | 'md' | 'lg';}export const Card = forwardRef<HTMLDivElement, CardProps>(({  variant = 'default',  padding = 'md',  className = '',  children,  ...props}, ref) => {  const baseClasses = 'bg-[#101827] border border-white/8 rounded-2xl shadow-[0_4px_20px_rgba(0,0,0,0.35)] transition-all duration-150 ease-out';  const paddingClasses = {    none: 'p-0',    sm: 'p-4',    md: 'p-6',    lg: 'p-8',  }[padding];  const variantClasses = {    default: '',    elevated: 'bg-[#141F36] border-white/12 shadow-[0_8px_30px_rgba(0,0,0,0.50)]',    interactive: 'hover:border-white/16 hover:shadow-[0_6px_24px_rgba(0,0,0,0.45)] hover:-translate-y-px cursor-pointer active:scale-[0.99]',    accent: 'border-[rgba(0,212,170,0.30)] shadow-[0_4px_20px_rgba(0,212,170,0.1)]',  }[variant];  return (    <div      ref={ref}      className={cn(baseClasses, paddingClasses, variantClasses, className)}      {...props}    >      {children}    </div>  );});Card.displayName = 'Card';