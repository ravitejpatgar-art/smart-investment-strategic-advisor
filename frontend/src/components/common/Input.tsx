import React, { forwardRef } from 'react';import { cn } from '../../utils/cn';export interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {  label?: string;  error?: string;  helperText?: string;  leftIcon?: React.ReactNode;  rightIcon?: React.ReactNode;}export const Input = forwardRef<HTMLInputElement, InputProps>(({  label,  error,  helperText,  leftIcon,  rightIcon,  className = '',  id,  disabled,  ...props}, ref) => {  const inputId = id || (label ? label.toLowerCase().replace(/\s+/g, '-') : undefined);  return (    <div className="flex flex-col gap-1.5 w-full">      {label && (        <label          htmlFor={inputId}          className="text-xs font-semibold text-[#A0AEC0] uppercase tracking-wider select-none"        >          {label}        </label>      )}      <div className="relative flex items-center w-full">        {leftIcon && (          <div className="absolute left-3.5 text-[#5A667A] pointer-events-none flex items-center">            {leftIcon}          </div>        )}        <input          ref={ref}          id={inputId}          disabled={disabled}          className={cn(            'w-full px-4 py-2.5 bg-[#0F172A]/50 border border-white/8 rounded-xl text-white font-sans text-sm transition-all duration-200 outline-none placeholder:text-[#5A667A]',            'focus:border-[#00D4AA] focus:ring-2 focus:ring-[#00D4AA]/15 focus:bg-[#0F172A]/80',            'disabled:opacity-50 disabled:cursor-not-allowed',            leftIcon ? 'pl-10' : '',            rightIcon ? 'pr-10' : '',            error ? 'border-red-500/50 focus:border-red-500 focus:ring-red-500/15' : '',            className          )}          {...props}        />        {rightIcon && (          <div className="absolute right-3.5 text-[#5A667A] flex items-center">            {rightIcon}          </div>        )}      </div>      {error ? (        <span className="text-xs text-red-400 font-medium">{error}</span>      ) : helperText ? (        <span className="text-xs text-[#5A667A]">{helperText}</span>      ) : null}    </div>  );});Input.displayName = 'Input';